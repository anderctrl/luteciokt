version: '3.8'

services:
  bot:
    build: .
    container_name: lutecio_bot
    restart: unless-stopped
    ports:
      - "${DASHBOARD_PORT}:${DASHBOARD_PORT}"
    environment:
      - DISCORD_TOKEN=${DISCORD_TOKEN}
      - LAVALINK_URL=${LAVALINK_URL}
      - LAVALINK_PASS=${LAVALINK_PASS}